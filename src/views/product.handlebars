<nav class="navbar fixed-top navbar-expand-lg navbar-light bg-light">
        <div class="container px-4 px-lg-5">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-lg-4">
                    <li class="nav-item"><a class="nav-link active" aria-current="page" href="/products">Home</a></li>
                    {{#if userAdmin}}
                        <li class="nav-item"><a class="nav-link active" href="/usersAdmin">Administración de Usuarios</a></li>
                    {{/if}}
                </ul>
                <button class="btn btn-outline-dark" id="btnVaciarCarrito" type="submit">
                    <i class="bi bi-recycle"></i> Vaciar Carrito
                </button>
                <button class="btn btn-outline-dark" id="btnCarrito" type="submit">
                    <i class="bi-cart-fill me-1"></i>
                    <span id="idContadorCarrito" class="ms-1 rounded-pill">0</span>
                    <span class="ms-1 rounded-pill">Total: <span id="idTotalCarrito">0</span> $</span>
                </button>
            </div>
        </div>
</nav>
<header class="bg-light">
    <div class="p-4 mb-2 bg-light text-dark">
        <div class="text-center text-dark">
            <p class="display-6 fw-bolder mt-4">Bienvenido {{user.first_name}}</p>
        </div>
        <div class="d-flex bd-highlight">
            <div class="p-0 flex-grow-1 bd-highlight">Email: {{user.email}} </div>
            <div class="p-0 flex-shrink-1 bd-highlight">Role: {{user.role}}</div>
        </div>
    </div>
</header>

<section class="container">
    <div class="row align-items-center ">
        <div class="col rounded-1">
            <figure class="figure">
                {{#if product.thumbnail}}
                    {{#if product.thumbnail.[0]}}
                        <img class="figure-img img-fluid rounded" src="{{product.thumbnail.[0]}}" alt="Imagen de Articulo">
                    {{else}}
                        <img src="/img/Imagen_no_disponible.png" class="figure-img img-fluid rounded" alt="Imagen de Articulo">
                    {{/if}}
                {{else}}
                    <img src="/img/Imagen_no_disponible.png" class="figure-img img-fluid rounded" alt="Imagen de Articulo">
                {{/if}}
            </figure>
        </div>
        <div class="col border border-dark rounded-1">
            <p>SKU: {{product.code}}</p>
            <br>
            <p>{{product.title}}</p>
            <br>
            <h5 class="fw-bolder" style="color: black">{{product.description}}</h5>
            <br>
            <p>Stock: {{product.stock}}</p>
            <br>
            <p class="text-center">$ {{product.price}}</p>
        </div>
        <div class="col rounded-1">
            <div class="text-center">
                <a onclick="sumarCantidad({{product.stock}})" id="idsumaProducto1" class="btn  mt-auto">
                    <img src="/img/sumar.png">
                </a>
                <span id="idContadorProducto" class="badge bg-dark2 text-dark ms-1">0</span>
                <a onclick="restarCantidad()" id="idRestaProducto1" class="btn  mt-auto">
                    <img src="/img/restar.png">
                </a>
            </div>
            <div class="text-center">
                <button onclick="agregarProducto('{{product._id}}')" type="button" class="btn btn-secondary">Agregar al Carrito</button>
            </div>
            <br>
            <div class="text-center">
                <a href="/products"><button type="button" class="btn btn-secondary">Volver a la Tienda</button></a>
            </div>
        </div>
    </div>
</section>
<script src="/js/product.js"></script>
<script src="/js/cart.js"></script>